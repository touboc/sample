[role]
act as an american native and IT engineer and an expert for typescript langaue.
[task]
write a script that implement the below requirement.
- create  a excel file named test.xlsx.
- write hello to the cell at "A1" in sheet("sheet1" )
-

npm pack @github/copilot
npm install -g xlsx
npm install xlsx
npm install --save-dev @types/xlsx

[command]
- mkdir test-xls
- touch create-xls.ts
- copy the code from copilot and past to file `create-xls.ts`
- npm install xlsx
- npm install --save-dev @types/xlsx

[issue]
explain the below command 
```
npm install --save-dev @types/xlsx
```
[good/prompts]
What the command actually does

for the test, i do the following process.
[command-process]
- mkdir test-xls
- touch create-xls.ts
- copy the code from copilot and past to file `create-xls.ts`
- npm install xlsx
- npm install --save-dev @types/xlsx

help me to improve the process
so that it is more professinal.

[command]
mkdir ts-excel-writer
cd ts-excel-writer
npm init -y
# can i download  typescript package from internet with edge
# how to check the typescript version installed
npm install --save-dev typescript
cd /d C:\Users\user\Downloads
cd /d C:\Users\user\ts-excel-writer
npm install -g typescript-6.0.3.tgz
npx tsc --init
npx tsc --version

mkdir src
touch src/create-xls.ts

# xlsx package for npm
https://www.npmjs.com/package/xlsx
[x]npm install -g sheetjs-0.18.5.zip
[o]npm install -g sheetjs-0.18.5.tar.gz
npm install xlsx

# how to install @types/xlsx with sheetjs-0.18.5.tar.gz
https://www.jsdelivr.com/package/npm/@types/xlsx
npm pack @types/xlsx
npm install --save-dev @types/xlsx
npm install -g xlsx-0.0.36.tgz
npm install node-26.1.2.tgz
Add a build script
src/create-xls.ts
npm run build
npm start

[error]
npm ls xlsx
npm ls @types/node
npm install ./sheetjs-0.18.5.zip